Step 1 — initialize repo (ONLY once)		git init
git config --global user.name "Alaa Eddine"
git config --global user.email "your_email@example.com"

git add .
git commit -m "v1: initial api and kafka integration"

git branch -M main
git push -u origin main
